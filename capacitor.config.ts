import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ComponentesReutilizables_Insumo2_Rivera',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
